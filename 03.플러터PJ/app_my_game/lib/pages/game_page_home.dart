import 'package:flutter/material.dart';

class MyGameHome extends StatelessWidget {
  const MyGameHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}