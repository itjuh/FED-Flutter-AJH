[ 오늘의 일정 : 12월 22일(금) ]
------------------------------
1. 플러터 학습
   - 다트문법 기본편 : 추상클래스

붕어빵 : 메모리당 인스턴스 된 인스턴스 객체
붕어빵 기계 : 목적에 맞게 구성해주는 클래스

컴포지션방법과 믹스인방법 차이 구분하기

오버라이드 : 올라탐
정의한 것을 덮어씀

다트는 오버로드가 없음!!!!!
오버로드 : 불러오기
같은 이름인데 여러 행동을 수행함
$().attr('')
$().attr('','')


겟터 셋터
final 상수 일 경우 화면에 출력 전에 세팅하면 괜찮음
late는 나중에 할당한다는 느낌으로 키워드 작성해야함
late 변수형 셋팅변수명;
변수형 get 변수명 => 셋팅변수명;
set 변수명(변수형 변수명1) -> 셋팅변수명 = 변수명1

추상클래스
설계도!
implements 모두 구현(정의 된 것도)
extends 구현 안된 부분만
mixin 상속 가능
on 은 mixin 제한조건

플러터 생성하기
lib -> main.dart가 기본경로